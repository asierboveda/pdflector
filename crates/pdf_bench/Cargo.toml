[package]
name = "pdf_bench"
version = "0.1.0"
edition.workspace = true

[dependencies]
pdf_core = { path = "../pdf_core" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "open_render"
harness = false

[[bench]]
name = "cache_scroll"
harness = false
