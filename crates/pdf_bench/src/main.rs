//! pdf_bench — benchmark harness (desktop and Android).
//! Real benchmarks arrive in Phase 0.5 (engine shootout, see docs/PLAN.md);
//! this stub only proves the crate builds and links against pdf_core.

fn main() {
    println!("pdf_bench: engine benchmarks land in Phase 0.5 (docs/PLAN.md)");
}
