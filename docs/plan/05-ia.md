# Fase 5 — IA con Ollama / Groq

> Milestone: `Fase 5 — IA con Ollama/Groq` · Issue: #29 · Estado: 🟡 Código OK, falta E2E

## Objetivo

Pregunta sobre el PDF responde con contenido real del documento (citas + nº página).

## Criterio de aceptación

- [x] `ai.rs` chunk_pages + OllamaClient/GroqClient (HTTP std::net, sin deps)
- [x] Panel chat en `pdf_app` (hilo fondo, llama3.2 localhost:11434)
- [x] `pdf_android` crop PNG → base64 → chat_vision
- [ ] E2E contra Ollama real (PC) y Groq API (tablet, requiere INTERNET + key)
- [ ] Validar que respuesta cita páginas reales del PDF

## Tareas

- [x] `crates/pdf_core/src/ai.rs`
- [ ] Instalar Ollama en PC, probar `cargo run -p pdf_app -- --chat`
- [ ] Configurar `groq_key.txt` en tablet y probar visión

## Cómo modificar

- Si quieres cambiar de Groq a otro provider: edita `ai.rs` y este fichero. Ollama local sigue siendo fallback.

## Referencias

- `crates/pdf_core/src/ai.rs`
